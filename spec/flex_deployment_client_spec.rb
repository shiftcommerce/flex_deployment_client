require "spec_helper"

describe FlexDeploymentClient do
  it "has a version number" do
    expect(FlexDeploymentClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
